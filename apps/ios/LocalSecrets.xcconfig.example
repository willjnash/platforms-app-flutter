// Copy to LocalSecrets.xcconfig in this directory (same folder as Base.xcconfig).
// LocalSecrets.xcconfig is gitignored — do not commit credentials.
// Value must be the full Authorization header value, including the "Basic " prefix.

RTT_BASIC_AUTH_HEADER = Basic YOUR_BASE64_CREDENTIALS_HERE
