// Default: no credentials (app shows setup instructions).
// Optional: add apps/ios/LocalSecrets.xcconfig (gitignored) with:
//   RTT_BASIC_AUTH_HEADER = Basic <base64(username:password)>
RTT_BASIC_AUTH_HEADER =
#include? "LocalSecrets.xcconfig"
